---
title: "Study3SSQ"
output:
  pdf_document: default
  html_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


```{r, include=FALSE}
  library(tidyverse)
  library(readr)
  library(janitor)
  library(emmeans)
  library(car)
  library(tidyr)
  library(stringr)
  library(lme4)
  library(dplyr)
  library(report)

```


```{r, include=FALSE}
#Set the working directory of the data ***HARD CODED ***
 #setwd("D:/Study2Data/qualtrics_data/SSQ")
SSQdataRaw = read.csv("C:/Users/thoma/OneDrive - University of Glasgow/Desktop/Study3/data/Qualtrics/SSQ/Study3SSQ.csv") %>%  
    select(starts_with('Q')) 
  
  #Format the column names by removing excess characters
  
SSQdataRaw[1,] <- sub(".*- ", "", SSQdataRaw[1,])
    
#change the row names to the column headers
SSQdataOrg <- row_to_names(SSQdataRaw, row_number = 1)
   
#remove pilot and test data  ***HARD-CODED***
SSQdataOrg <- SSQdataOrg[-c(1),] %>% 
  rename(participant = `Participant ID:`) %>% 
   rename(Timepoint = `Which timepoint is this?`)


```


```{r}
Counterbalancing <- read.csv("C:/Users/thoma/OneDrive - University of Glasgow/Desktop/Study3/Study3Counterbalancing.csv", header = T)

Counterbalancing <- Counterbalancing%>%  select(participant, Second, Third, Fourth, Fifth)

Counterbalancing <- na.omit(Counterbalancing)
  


```

```{r,include=FALSE}

#Recode Values so they are computable

SSQdata <- SSQdataOrg

SSQdata[SSQdata== '0 - None at all'] <- '0'
SSQdata[SSQdata== '1 - Mild'] <- '1'
SSQdata[SSQdata== '2 - Moderate'] <- '2'
SSQdata[SSQdata== '3 - Severe'] <- '3'

SSQdata[SSQdata== 'Pre-Experiment'] <- '1'
SSQdata[SSQdata== 'End of Control'] <- '2'
SSQdata[SSQdata== 'End of Block 1'] <- '3'
SSQdata[SSQdata== 'End of Block 2'] <- '4'
SSQdata[SSQdata== 'End of Block 3'] <- '5'
SSQdata[SSQdata== 'End of Experiment'] <- '6'

#Convert columns with ratings in to numeric
SSQdata[3:18] = lapply(SSQdata[3:18], FUN = function(y){as.numeric(y)})

```


```{r}
SSQdata <- left_join(SSQdata, Counterbalancing, by = 'participant') 
SSQdata$Block <- 'Baseline' 


SSQdata$Block <-  if_else(SSQdata$Timepoint == "6", SSQdata$Fifth,
                          if_else(SSQdata$Timepoint == "5", SSQdata$Fourth,
                                  if_else(SSQdata$Timepoint == "4", SSQdata$Third,
                                          if_else(SSQdata$Timepoint == "3", SSQdata$Second,
                                                  if_else(SSQdata$Timepoint == "2", "Control", 
                                                          if_else(SSQdata$Timepoint == "21",'Baseline', 'Baseline'))))))

SSQdata <- SSQdata %>% 
  select(-c(19:22))

SSQdata <- SSQdata %>% 
    dplyr::relocate(Block, .before = Timepoint)

```


```{r}
#Calculate the SSQ score weightings according to Kennedy et al 1993
SSQConversion <- SSQdata 

SSQConversion <- SSQConversion %>% 
  mutate(NauseaSum = 
           (SSQConversion$`General Discomfort` + 
            SSQConversion$`Increased Salivation` + 
            SSQConversion$Sweating + 
            SSQConversion$Nausea + 
            SSQConversion$`Difficulty Concentrating`+
            SSQConversion$`Stomach Awareness` +
            SSQConversion$Burping)) %>% 
  mutate(OculomotorSum = 
           (SSQConversion$`General Discomfort` +
            SSQConversion$Fatigue +
            SSQConversion$Headache +
            SSQConversion$`Eye Strain` +
            SSQConversion$`Difficulty Focusing` +
            SSQConversion$`Difficulty Concentrating`+
            SSQConversion$`Blurred Vision`)) %>% 
  mutate(DisorientationSum =
           (SSQConversion$`Difficulty Focusing` +
            SSQConversion$Nausea +
            SSQConversion$`Fullness of Head` +
            SSQConversion$`Blurred Vision` +
            SSQConversion$`Dizzy (Eyes Open)` +
            SSQConversion$`Dizzy ( Eyes Closed)`+
            SSQConversion$Vertigo)) 

#Calculate the final weighted SSQ scores

  SSQConversion <- SSQConversion %>% 
      mutate(NauseaWeight = (SSQConversion$NauseaSum * 9.54)) %>% 
      mutate(OculomotorWeight = (SSQConversion$OculomotorSum*7.58)) %>% 
      mutate(DisorientationWeight = (SSQConversion$DisorientationSum *13.92)) %>% 
      mutate(TotalSSQScore = 
               (SSQConversion$NauseaSum +
               SSQConversion$OculomotorSum +
               SSQConversion$DisorientationSum)*
               3.74)
  
 # write.csv(SSQConversion,"D:/Qualtrics_Data/Study1/SSQ/EXP1SSQSort.csv", row.names = FALSE)
  
  # SSQConversion <- SSQConversion %>% 
  #   
  #     subset(Block != "Tablet")

  
```

```{r}
#Summarise the descriptives for the converted scores
SSQConversionSummary <- SSQConversion %>% 
  
  group_by(Timepoint, Block) %>%  # can add Block here to split by condition
  summarise(
  n = n(),
  mean = mean(TotalSSQScore),
  sd = sd(TotalSSQScore),
  min = min(TotalSSQScore),
  max = max(TotalSSQScore))   
#Summarise the descriptives for the converted  Nausea scores
SSQNauseaSummary <- SSQConversion %>% 
  
  group_by(Timepoint, Block) %>% 
  summarise(
  n = n(),
  mean = mean(NauseaWeight),
  sd = sd(NauseaWeight),
  min = min(NauseaWeight),
  max = max(NauseaWeight))   
#Summarise the descriptives for the converted Oculomotor scores
SSQOculoSummary <- SSQConversion %>% 
  
  group_by(Timepoint, Block) %>% 
  summarise(
  n = n(),
  mean = mean(OculomotorWeight),
  sd = sd(OculomotorWeight),
  min = min(OculomotorWeight),
  max = max(OculomotorWeight))  
#Summarise the descriptives for the converted Disorientation scores
SSQDisoriSummary <- SSQConversion %>% 
  
  group_by(Timepoint, Block) %>% 
  summarise(
  n = n(),
  mean = mean(DisorientationWeight),
  sd = sd(DisorientationWeight),
  min = min(DisorientationWeight),
  max = max(DisorientationWeight)) 
```


```{r}
  ########################END OF DATA WRANGLING########################
  #####################################################################
```

        
```{r}
#Visualise the spread of the data
ggplot(SSQConversion, aes(x = TotalSSQScore, fill = Timepoint))+
  geom_histogram(binwidth = 3)

```
           
```{r}
#Create violin plots for each of the Weighted totals to show the spread of the data
ggplot(SSQConversion, aes(Timepoint, TotalSSQScore, fill = Block ))+
  geom_violin(alpha = 0.5)+
  labs(title = "Total SSQ Score")

ggplot(SSQConversion, aes(Timepoint, OculomotorWeight, fill = Block ))+
  geom_violin(alpha = 0.5) +
  labs(title = "Oculomotor Score")
ggplot(SSQConversion, aes(Timepoint, NauseaWeight, fill = Block ))+
  geom_violin(alpha = 0.5) +
  labs(title = "Nausea score")
ggplot(SSQConversion, aes(Timepoint, DisorientationWeight, fill = Block ))+
  geom_violin(alpha = 0.5)+
  labs(title = "Disorientation Score")
```


```{r}
ggplot(SSQConversionSummary,  aes(x = Timepoint, y = mean, colour = Block, group = Block, shape = Block))+
  #geom_point (size = 4.5, aes(color = Condition), shape = 8) +
  geom_point (size = 3.5, position = position_dodge(width = 0.5)) +
  scale_shape_manual(values=c(0,15,1,16,2,17,18,19,20,21,22,23,24))+
  scale_color_brewer(palette="Dark2")+
  geom_line(position = position_dodge(width = 0.5))+
  labs(x = "Block",
      y = "Total SSQ Score",
      title = "Total SSQ Score",
      tag = "")+  
  ylim(0,30) +
    scale_fill_hue(l = 2)+
   
    geom_errorbar(aes(ymin = mean - (sd/2), ymax = mean + (sd/2)),
                width = 0.2, position = position_dodge(width = 0.5))+



  theme_bw()
```




```{r}
#Create interaction plots for the mean of each condition at each of the 3 timepoints
#**pch = type of shape on the point graph
interaction.plot(x.factor =  SSQConversion$Timepoint,
                 trace.factor = SSQConversion$Block,
                 response = SSQConversion$TotalSSQScore,
                 fun = mean,
                 ylim = (c(0,25)),
                 xlab = "Timepoint",
                 ylab = "Total SSQ Score",
                 type= "b",
                 col=c("red","blue"),
                 pch=c(19,17,15),
                 trace.label = "Total SSQ Score",
                 fixed=T,
                 leg.bty ="o")
interaction.plot(x.factor =  SSQConversion$Timepoint,
                 trace.factor = SSQConversion$Timepoint,
                 response = SSQConversion$NauseaWeight,
                 fun = mean,
                 ylim = (c(0,20)),
                 xlab = "Timepoint",
                 ylab = "Nausea subscale",
                 type= "b",
                 col=c("red","blue"),
                 pch=c(19,17,15),
                 trace.label = "Nausea",
                 fixed=T,
                 leg.bty ="o")
interaction.plot(x.factor =  SSQConversion$Timepoint,
                 trace.factor = SSQConversion$Timepoint,
                 response = SSQConversion$OculomotorWeight,
                 fun = mean,
                 ylim = (c(0,20)),
                 xlab = "Timepoint",
                 ylab = "Oculomotor subscale",
                 type= "b",
                 col=c("red","blue"),
                 pch=c(19,17,15),
                 trace.label = "Oculomotor",
                 fixed=T,
                 leg.bty ="o")
interaction.plot(x.factor =  SSQConversion$Timepoint,
                 trace.factor = SSQConversion$Timepoint,
                 response = SSQConversion$DisorientationWeight,
                 fun = mean,
                 ylim = (c(0,20)),
                 xlab = "Timepoint",
                 ylab = "Disorientation subscale",
                 type= "b",
                 col=c("red","blue"),
                 pch=c(19,17,15),
                 trace.label = "Disorientation",
                 fixed=T,
                 leg.bty ="o")
```

```{r}
# Generate linear modals for each of the weighted scores
SSQModel = lm(TotalSSQScore ~ Timepoint*Block,data = SSQConversion)


test = aov(TotalSSQScore ~ Timepoint,data = SSQConversion)

summary(test)

Anova(SSQModel,
      type = "II")

```
```{r}
#Calculate comparison means for Overall scores
#SSQmarginal = emmeans(SSQModel,~Timepoint)
#pairs(SSQmarginal, adjust="tukey")

```
 
```{r}
# Generate linear modals for Oculomotor subscale
# 
# OcuModel = lm(OculomotorWeight ~ Timepoint, data = SSQConversion)
# 
# Anova(OcuModel,
#       type = "II")

```

```{r}
#Calculate comparison means for Oculomotor subscale
# 
# Ocumarginal = emmeans(OcuModel, ~Timepoint)
# pairs(Ocumarginal, adjust="tukey")

```
 
```{r}
# # Generate linear modals for Nausea subscale
# NauModel = lm(NauseaWeight ~ Timepoint,data = SSQConversion)
# 
# Anova(NauModel,
#       type = "II")
```

```{r}
#Calculate comparison means for Nausea subscale
# 
# Naumarginal = emmeans(NauModel, ~Timepoint)
# pairs(Naumarginal, adjust="tukey")
# 
# #Significatn effect of conidtion, but is meaningless in the comparisons

```
 
```{r}
# #Generate linear modals for Disorientation  subscale
# DisModel = lm(DisorientationWeight ~ Timepoint,data = SSQConversion)
# 
# Anova(DisModel,
#       type = "II")
```

```{r}
#Calculate comparison means for Disorientation subscale 
# 
# Dismarginal = emmeans(DisModel, ~Timepoint)
# pairs(Dismarginal, adjust="tukey")

```
```{r}
TotalSSQNullmodel <- lmer(data = SSQConversion, TotalSSQScore ~ (1|participant))
TotalSSQmodel1 <- lmer(data = SSQConversion, TotalSSQScore ~Timepoint + (1|participant))
TotalSSQmodel2 <- lmer(data = SSQConversion, TotalSSQScore ~Timepoint + Block + (1|participant))

TotalSSQmodel3 <- lmer(data = SSQConversion, TotalSSQScore ~Timepoint * Block + (1|participant))

anova(TotalSSQmodel1, TotalSSQNullmodel)

summary(TotalSSQNullmodel)
```


```{r}
TotalSSQresults <- report(TotalSSQmodel1, CI = 95)

#summary(results)
print(TotalSSQresults)
```
```{r}

```


```{r}
##################  CONVERT DATA TO LOOK AT DIFFERENCES OVER TIME ###########
#############################################################################
```

```{r}
#Convert the dataset to look out differences over time

SSQKPConversion <- SSQConversion %>% 
  
  pivot_wider(names_from = Timepoint, values_from = 23:26) 
SSQKPConversion <- SSQKPConversion %>% mutate(across(22:45, ~replace(., lengths(.) == 0, NA))) 
SSQKPConversion$TotalSSQScore_1 <- as.numeric(SSQKPConversion$TotalSSQScore_1)
SSQKPConversion$TotalSSQScore_2 <- as.numeric(SSQKPConversion$TotalSSQScore_2)
SSQKPConversion$TotalSSQScore_3 <- as.numeric(SSQKPConversion$TotalSSQScore_3)
SSQKPConversion$TotalSSQScore_4 <- as.numeric(SSQKPConversion$TotalSSQScore_4)
SSQKPConversion$TotalSSQScore_5 <- as.numeric(SSQKPConversion$TotalSSQScore_5)
SSQKPConversion$TotalSSQScore_6 <- as.numeric(SSQKPConversion$TotalSSQScore_6)

SSQKPConversion$OculomotorWeight_1 <- as.numeric(SSQKPConversion$OculomotorWeight_1)
SSQKPConversion$OculomotorWeight_2 <- as.numeric(SSQKPConversion$OculomotorWeight_2)
SSQKPConversion$OculomotorWeight_3 <- as.numeric(SSQKPConversion$OculomotorWeight_3)
SSQKPConversion$OculomotorWeight_4 <- as.numeric(SSQKPConversion$OculomotorWeight_4)
SSQKPConversion$OculomotorWeight_5 <- as.numeric(SSQKPConversion$OculomotorWeight_5)
SSQKPConversion$OculomotorWeight_6 <- as.numeric(SSQKPConversion$OculomotorWeight_6)

SSQKPConversion$NauseaWeight_1 <- as.numeric(SSQKPConversion$NauseaWeight_1)
SSQKPConversion$NauseaWeight_2 <- as.numeric(SSQKPConversion$NauseaWeight_2)
SSQKPConversion$NauseaWeight_3 <- as.numeric(SSQKPConversion$NauseaWeight_3)
SSQKPConversion$NauseaWeight_4 <- as.numeric(SSQKPConversion$NauseaWeight_4)
SSQKPConversion$NauseaWeight_5 <- as.numeric(SSQKPConversion$NauseaWeight_5)
SSQKPConversion$NauseaWeight_6 <- as.numeric(SSQKPConversion$NauseaWeight_6)

SSQKPConversion$DisorientationWeight_1 <- as.numeric(SSQKPConversion$DisorientationWeight_1)
SSQKPConversion$DisorientationWeight_2 <- as.numeric(SSQKPConversion$DisorientationWeight_2)
SSQKPConversion$DisorientationWeight_3 <- as.numeric(SSQKPConversion$DisorientationWeight_3)
SSQKPConversion$DisorientationWeight_4 <- as.numeric(SSQKPConversion$DisorientationWeight_4)
SSQKPConversion$DisorientationWeight_5 <- as.numeric(SSQKPConversion$DisorientationWeight_5)
SSQKPConversion$DisorientationWeight_6 <- as.numeric(SSQKPConversion$TotalSSQScore_6)

SSQKPConversion<- SSQKPConversion %>% 
  select(participant, Block)
```


```{r}

SSQKPConversion[, TotalSSQChange := Reduce('+', .SD), .SDcols = 40:45]


SSQKPConversion <- SSQKPConversion %>% 
  mutate(TotalSSQScore =  rowSums(select(TotalSSQScore_1, TotalSSQScore_2,TotalSSQScore_3,TotalSSQScore_4,TotalSSQScore_5,TotalSSQScore_6), na.rm = T))
%>%     mutate(TotalOculoScore =  rowSums(dplyr::select(.,OculomotorWeight_1, OculomotorWeight_2,OculomotorWeight_3,OculomotorWeight_4,OculomotorWeight_5,OculomotorWeight_6), na.rm = T)) %>% mutate(TotalNauseaScore =  rowSums(dplyr::select(.,NauseaWeight_1, NauseaWeight_2,NauseaWeight_3,NauseaWeight_4,NauseaWeight_5,NauseaWeight_6), na.rm = T)) %>% mutate(TotalDisoriScore =  rowSums(dplyr::select(.,DisorientationWeight_1, DisorientationWeight_2,DisorientationWeight_3,DisorientationWeight_4,DisorientationWeight_5,DisorientationWeight_6), na.rm = T)) %>% 
  mutate(Timepoint = SSQConversion$Timepoint) %>% 
  select(participant,Timepoint, Block, 141:144)

SSQKPConversion$participant <- as.factor(SSQKPConversion$participant)
SSQKPConversion$Block <- as.factor(SSQKPConversion$Block)

SSQKPConversion$Timepoint[SSQKPConversion$Timepoint== '1'] <- 'Baseline'
SSQKPConversion$Timepoint[SSQKPConversion$Timepoint== '2'] <- 'T1'
SSQKPConversion$Timepoint[SSQKPConversion$Timepoint== '3'] <- 'T2'
SSQKPConversion$Timepoint[SSQKPConversion$Timepoint== '4'] <- 'T3'
SSQKPConversion$Timepoint[SSQKPConversion$Timepoint== '5'] <- 'T4'
SSQKPConversion$Timepoint[SSQKPConversion$Timepoint== '6'] <- 'T5'

#SSQKPConversion$Timepoint <- as.factor(SSQKPConversion$Timepoint)


```

```{r}
## Reformat data and wrtie csv file to to be read in JASP  


SSQKPConversionTotalSSQ <- SSQKPConversion %>% 
  select(-(5:7)) %>% 
  mutate(TotalSSQChange = TotalSSQScore)

SSQKPConversionTotalSSQ$Timepoint <- factor(SSQKPConversionTotalSSQ$Timepoint, levels = c("Baseline", "T1","T2", "T3", "T4", "T5"))
SSQKPConversionTotalSSQ$TotalSSQChange <- 0
TotalSSQChange <- diff(SSQKPConversionTotalSSQ$TotalSSQScore) 
TotalSSQChange <- append(0, TotalSSQChange)
 
SSQKPConversionTotalSSQ$TotalSSQChange <- TotalSSQChange 
SSQKPConversionTotalSSQ$TotalSSQScoreChange <- if_else(SSQKPConversionTotalSSQ$Timepoint == "Baseline", 0, SSQKPConversionTotalSSQ$TotalSSQChange)



```


```{r}
##Create summary tables for the Total SSQ 
#'
SSQKPConversionTotalSSQSummary <- SSQKPConversionTotalSSQ %>% 
  
  group_by(Timepoint, Block) %>% 
  summarise(
  n = n(),
  mean = mean(TotalSSQScoreChange, na.rm = T),
  sd = sd(TotalSSQScoreChange, na.rm = T)
  )   

SSQKPConversionTotalSSQBlockSummary <- SSQKPConversionTotalSSQ %>% 
  
  group_by(Timepoint, Block) %>% 
  summarise(
  n = n(),
  mean = mean(Value, na.rm = T),
  sd = sd(Value, na.rm = T)
  )   

```

```{r}
ggplot(SSQKPConversionTotalSSQ, aes(Timepoint, TotalSSQScoreChange, colour = Block, group = Block ))+
  geom_boxplot()+
  labs(title = "Total SSQ Score")

interaction.plot(x.factor =  SSQKPConversionTotalSSQSummary$Timepoint,
                 trace.factor = SSQKPConversionTotalSSQSummary$Block,
                 response = SSQKPConversionTotalSSQSummary$mean,
                 fun = mean,
                 ylim = c(-10, 20),
                 xlab = "Timepoint",
                 ylab = "Total SSQ Score",
                 type= "b",
                 col=c("red","blue"),
                 pch=c(19,17,15),
                 trace.label = "Total SSQ Score",
                 fixed=F,
                 leg.bty ="o")
```


```{r}
ggplot(SSQKPConversionTotalSSQBlockSummary,  aes(x = Timepoint, y = mean, colour = Block, group = 1))+
  #geom_point (size = 4.5, aes(color = Condition), shape = 8) +
  geom_point (size = 3.5, shape = 8) +
  labs(x = "Block",
      y = "Total SSQ Score",
      title = "Total SSQ Score",
      tag = "")+  
  ylim(-10,20) +
    geom_errorbar(aes(ymin = mean - (sd/2), ymax = mean + (sd/2)),
                width = 0.2, 
                position = position_dodge(0.9))+
  geom_hline(yintercept = 0, linetype = "dashed")+

  theme_bw()
```

```{r}
KPSSQModelTotalSSQ = aov(Value ~ Timepoint,
              data = SSQKPConversionTotalSSQ)

Anova(KPSSQModelTotalSSQ,
      type = "II")
```

```{r}
KPSSQModelTotalSSQ = emmeans(KPSSQModelTotalSSQ, ~Timepoint)
pairs(KPSSQModelTotalSSQ, adjust="tukey")

```


```{r}
TotalKPSSQNullmodel <- lmer(data = SSQKPConversionTotalSSQ, Value ~ (1|participant))
TotalKPSSQmodel1 <- lmer(data = SSQKPConversionTotalSSQ, Value ~Timepoint + (1|participant))

anova(TotalKPSSQNullmodel, TotalKPSSQmodel1)

summary(TotalKPSSQmodel1)

```
```{r}
library(report)
TotalSSQresults <- report(TotalKPSSQmodel1, CI = 95)

print(TotalSSQresults)


```



```{r}
#############################################################################
```

```{r}
SSQKPConversionOculo <- pivot_longer(data = SSQKPConversionTotal,
                          cols = c(8:13)) %>% 
  select(c(1,20,21))

colnames(SSQKPConversionOculo)[colnames(SSQKPConversionOculo) %in% c("name", "value")] <- c("Timepoint", "Value") 
  
SSQKPConversionOculo[c('Measure', "Timepoint")] <- str_split_fixed(SSQKPConversionOculo$Timepoint, "_", 2)

  #write.csv(SSQKPConversionOculo,"D:/Qualtrics_Data/Study2/SSQ/EXP1SSQSortKPOculo.csv", row.names = FALSE)

```

```{r}

SSQKPConversionOculoSummary <- SSQKPConversionOculo %>% 
  
  group_by(Timepoint) %>% 
  summarise(
  n = n(),
  mean = mean(Value, na.rm = T),
  sd = sd(Value, na.rm = T)
  )   


```

```{r}
ggplot(SSQKPConversionOculo, aes(Timepoint, Value, fill = Timepoint ))+
  geom_violin(alpha = 0.5)+
  labs(title = "Oculomotor")

interaction.plot(x.factor =  SSQKPConversionOculoSummary$Timepoint,
                 trace.factor = SSQKPConversionOculoSummary$Timepoint,
                 response = SSQKPConversionOculoSummary$mean,
                 fun = mean,
                 ylim = c(0, 10),
                 xlab = "Timepoint",
                 ylab = "Change in Oculomotor subscore",
                 type= "b",
                 col=c("red","blue"),
                 pch=c(19,17,15),
                 trace.label = "Oculomotor",
                 fixed=F,
                 leg.bty ="o")

```

```{r}
KPSSQModelOculo = lm(Value ~ Timepoint,data = SSQKPConversionOculo)

Anova(KPSSQModelOculo,
      type = "II")

```


```{r}
OculoNullmodel <- lmer(data = SSQKPConversionOculo, Value ~ (1|participant))

Oculomodel1 <- lmer(data = SSQKPConversionOculo, Value ~Timepoint + (1|participant))

anova(Oculomodel1, OculoNullmodel)

summary(Oculomodel1)
```
```{r}
Oculoresults <- report(Oculomodel1, CI = 95)

#summary(results)
print(Oculoresults)
```

```{r}
#############################################################################
```


```{r}
SSQKPConversionNausea <- pivot_longer(data = SSQKPConversionTotal,
                          cols = c(14:19)) %>% 
  select(c(1,20,21))

colnames(SSQKPConversionNausea)[colnames(SSQKPConversionNausea) %in% c("name", "value")] <- c("Timepoint", "Value") 
  
SSQKPConversionNausea[c('Measure', "Timepoint")] <- str_split_fixed(SSQKPConversionNausea$Timepoint, "_", 2)

 # write.csv(SSQKPConversionNausea,"D:/Qualtrics_Data/Study1/SSQ/EXP1SSQSortKPNausea.csv", row.names = FALSE)
```


```{r}
SSQKPConversionNauseaSummary <- SSQKPConversionNausea %>% 
  
  group_by(Timepoint) %>% 
  summarise(
  n = n(),
  mean = mean(Value, na.rm = T),
  sd = sd(Value, na.rm = T)
  )   


```

```{r}
ggplot(SSQKPConversionNausea, aes(Timepoint, Value, fill = Timepoint ))+
  geom_violin(alpha = 0.5)+
  labs(title = "Nausea")

interaction.plot(x.factor =  SSQKPConversionNauseaSummary$Timepoint,
                 trace.factor = SSQKPConversionNauseaSummary$Timepoint,
                 response = SSQKPConversionNauseaSummary$mean,
                 fun = mean,
                 ylim = c(0, 6),
                 xlab = "Timepoint",
                 ylab = "Score",
                 type= "b",
                 col=c("red","blue"),
                 pch=c(19,17,15),
                 trace.label = "Nausea",
                 fixed=F,
                 leg.bty ="o")
```

```{r}

KPSSQModelNausea = lm(Value ~ Timepoint,
              data = SSQKPConversionNausea)

Anova(KPSSQModelNausea,
      type = "II")
```
```{r}
NauseaNullmodel <- lmer(data = SSQKPConversionNausea, Value ~ (1|participant))

Nauseamodel1 <- lmer(data = SSQKPConversionNausea, Value ~ Timepoint +(1|participant))

anova(NauseaNullmodel, Nauseamodel1)

summary(NauseaNullmodel)
```
```{r}
Nausearesults <- report(NauseaNullmodel, CI = 95)

#summary(results)
print(Nausearesults)
```


```{r}
#############################################################################
```

```{r}
SSQKPConversionDisori <- pivot_longer(data = SSQKPConversionTotal,
                          cols = c(20:25)) %>% 
  select(c(1,20,21))

colnames(SSQKPConversionDisori)[colnames(SSQKPConversionDisori) %in% c("name", "value")] <- c("Timepoint", "Value") 
  
SSQKPConversionDisori[c('Measure', "Timepoint")] <- str_split_fixed(SSQKPConversionDisori$Timepoint, "_", 2)


  #write.csv(SSQKPConversionDisori,"D:/Qualtrics_Data/Study1/SSQ/EXP1SSQSortKPDisori.csv", row.names = FALSE)

```


```{r}

SSQKPConversionDisoriSummary <- SSQKPConversionDisori %>% 
  
  group_by(Timepoint) %>% 
  summarise(
  n = n(),
  mean = mean(Value, na.rm = T),
  sd = sd(Value, na.rm = T)
  )   


```

```{r}
ggplot(SSQKPConversionDisori, aes(Timepoint, Value, fill = Timepoint ))+
  geom_violin(alpha = 0.5)+
  labs(title = "Disorientation")

interaction.plot(x.factor =  SSQKPConversionDisoriSummary$Timepoint,
                 trace.factor = SSQKPConversionDisoriSummary$Timepoint,
                 response = SSQKPConversionDisoriSummary$mean,
                 fun = mean,
                 ylim = c(-5 , 10),
                 xlab = "Timepoint",
                 ylab = "Change in Disorientation subscore",
                 type= "b",
                 col=c("red","blue"),
                 pch=c(19,17,15),
                 trace.label = "Disorientation",
                 fixed=F,
                 leg.bty ="o")
```

```{r}
KPSSQModelDisori = lm(Value ~ Timepoint,
              data = SSQKPConversionDisori)

Anova(KPSSQModelDisori,
      type = "II")

```

```{r}
DisoriNullmodel <- lmer(data = SSQKPConversionDisori, Value ~ (1|participant))

Disorimodel1 <- lmer(data = SSQKPConversionDisori, Value ~Timepoint + (1|participant))

anova(DisoriNullmodel, Disorimodel1)

summary(Disorimodel1)
```
```{r}
Disoriresults <- report(Disorimodel1, CI = 95)

#summary(results)
print(Disoriresults)
```

